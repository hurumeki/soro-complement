# 8. Data Management (Local Storage)

```javascript
// Data to persist
{
  highScores: {
    practice:  Number,  // Practice (2 digits)
    easy:      Number,  // Easy (2 digits)
    normal:    Number,  // Normal (3 digits)
    hard:      Number,  // Hard (4 digits)
    challenge: Number   // Challenge (5 digits)
  },
  settings: {
    soundEnabled:    Boolean,
    vibrationEnabled: Boolean
  }
}
```
